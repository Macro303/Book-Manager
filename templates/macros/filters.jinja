{% from "macros/forms.jinja" import input %}
{% macro author(action) %}
<div class="box">
  <h1 class="title is-5">Filter Authors</h1>
  <form accept-charset="UTF-8" action="{{ action }}" method="GET">
    {{ input("Name", "name", value=filters['name'] or '') }}
    <div class="buttons is-centered">
      <button class="button is-primary is-rounded">
        <span class="icon is-medium">
          <i class="fas fa-lg fa-filter"></i>
        </span>
        <span>Filter</span>
      </button>
    </div>
  </form>
</div>
{% endmacro %}



{% macro book(action) %}
<div class="box">
  <h5 class="title is-5">Filter Books</h5>
  <form accept-charset="UTF-8" action="{{ action }}" method="GET">
    <div class="field">
      <label class="label">Author</label>
      <div class="control is-expanded">
        <div class="select is-fullwidth is-rounded">
          <select name="author">
            <option value=0{% if not filters['author_id'] %} selected{% endif %}>Select Author</option>
            {% for _author in author_list %}
            <option value={{ _author.author_id }}{% if filters['author_id'] == _author.author_id %} selected{% endif %}>{{ _author.name }}</option>
            {% endfor %}
          </select>
        </div>
      </div>
    </div>
    <div class="field">
      <label class="label">Format</label>
      <div class="control is-expanded">
        <div class="select is-fullwidth is-rounded">
          <select name="format">
            <option value=""{% if not filters['format'] %} selected{% endif %}>Select Format</option>
            {% for _format in format_list %}
            <option value="{{ _format }}"{% if filters['format'] == _format %} selected{% endif %}>{{ _format }}</option>
            {% endfor %}
          </select>
        </div>
      </div>
    </div>
    <div class="field">
      <label class="label">Series</label>
      <div class="control is-expanded">
        <div class="select is-fullwidth is-rounded">
          <select name="series">
            <option value=0{% if not filters['series_id'] %} selected{% endif %}>Select Series</option>
            {% for _series in series_list %}
            <option value={{ _series.series_id }}{% if filters['series_id'] == _series.series_id %} selected{% endif %}>{{ _series.title }}</option>
            {% endfor %}
          </select>
        </div>
      </div>
    </div>
    {{ input("Title", "title", value=filters['title'] or '') }}
    <div class="field">
      <label class="label">Publisher</label>
      <div class="control is-expanded">
        <div class="select is-fullwidth is-rounded">
          <select name="publisher">
            <option value=0{% if not filters['publisher_id'] %} selected{% endif %}>Select Publisher</option>
            {% for _publisher in publisher_list %}
            <option value={{ _publisher.publisher_id }}{% if filters['publisher_id'] == _publisher.publisher_id %} selected{% endif %}>{{ _publisher.name }}</option>
            {% endfor %}
          </select>
        </div>
      </div>
    </div>
    <div class="buttons is-centered">
      <button class="button is-primary is-rounded">
        <span class="icon is-medium">
          <i class="fas fa-lg fa-filter"></i>
        </span>
        <span>Filter</span>
      </button>
    </div>
  </form>
</div>
{% endmacro %}



{% macro publisher(action) %}
<div class="box">
  <h1 class="title is-5">Filter Publishers</h1>
  <form accept-charset="UTF-8" action="{{ action }}" method="GET">
    {{ input("Name", "name", value=filters['name'] or '') }}
    <div class="buttons is-centered">
      <button class="button is-primary is-rounded">
        <span class="icon is-medium">
          <i class="fas fa-lg fa-filter"></i>
        </span>
        <span>Filter</span>
      </button>
    </div>
  </form>
</div>
{% endmacro %}



{% macro user(action) %}
<div class="box">
  <h1 class="title is-5">Filter Users</h1>
  <form accept-charset="UTF-8" action="{{ action }}" method="GET">
    {{ input("Username", "username", value=filters['username'] or '') }}
    <div class="buttons is-centered">
      <button class="button is-primary is-rounded">
        <span class="icon is-medium">
          <i class="fas fa-lg fa-filter"></i>
        </span>
        <span>Filter</span>
      </button>
    </div>
  </form>
</div>
{% endmacro %}
