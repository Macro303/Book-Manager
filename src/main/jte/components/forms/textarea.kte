@param label: String
@param name: String
@param value: String? = null

<div class="field">
    <label class="label has-text-info">${label}</label>
    <div class="control is-expanded">
        <textarea class="textarea has-fixed-size" name="${name}" placeholder="${label}">${value}</textarea>
    </div>
</div>
